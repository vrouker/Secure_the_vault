// You are building a vault that requires three mathematical calculations to generate the three codes in a combination. Use the JavaScript console or the script block within an HTML page to create three variables. Each variable will be the result of each calculation (three in total). The combination of the lock is 10 - 40 - 39. You must use three different arithmetic operators that will generate each individual number, then display the combination on the HTML page, or in an alert popup.

// 1. create a variable equal to 10 using division
// 2. create a variable equal to 40 using multiplication
// 3. create a variable equal to 39 using subtraction
// 4. create a variable with the message: You have received this message because you have been chosen to open an important vault. Here is the secret combination:
// 5. build an alert that displays the message above

// STEP 1:
const numOne = 30/3

// STEP 2:
const numTwo = 20*2

//STEP 3:
const numThree = 40-1

// STEP 4:
const message = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:"

// STEP5:
alert(`${message} ${numOne}-${numTwo}-${numThree}`)

